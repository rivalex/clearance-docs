import{_ as i,o as a,c as o,a2 as r}from"./chunks/framework.BKG_UbFK.js";const m=JSON.parse('{"title":"Sicurezza","description":"","frontmatter":{},"headers":[],"relativePath":"it/guide/security.md","filePath":"it/guide/security.md"}'),t={name:"it/guide/security.md"};function l(n,e,s,c,d,p){return a(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="sicurezza" tabindex="-1">Sicurezza <a class="header-anchor" href="#sicurezza" aria-label="Permalink to &quot;Sicurezza&quot;">​</a></h1><p>Clearance è stato sottoposto a un audit di sicurezza dedicato pre-pubblicazione oltre a due successivi passaggi di hardening prima del rilascio. Punti salienti: limiti ceiling contro l&#39;escalation di privilegi su ogni percorso di concessione dei permessi (concessioni dirette e sincronizzazione a livello di ruolo allo stesso modo), override di contesto con priorità al diniego, un sanitizzatore SVG allow-list, un namespace di permessi <code>clearance-*</code> protetto, e direttive Blade sicure per gli ospiti.</p><h2 id="accesso-al-pannello-e-permessi-di-scrittura-granulari" tabindex="-1">Accesso al pannello e permessi di scrittura granulari <a class="header-anchor" href="#accesso-al-pannello-e-permessi-di-scrittura-granulari" aria-label="Permalink to &quot;Accesso al pannello e permessi di scrittura granulari&quot;">​</a></h2><p>L&#39;accesso in lettura all&#39;intero pannello richiede il permesso <code>clearance-access</code> (verificato tramite <code>can()</code>, mai <code>hasRole()</code>). Le operazioni di scrittura su ogni sezione sono protette da un permesso dedicato e delegabile - tutti creati da <code>clearance:install</code> e assegnati a <code>super_admin</code>:</p><table tabindex="0"><thead><tr><th>Permesso</th><th>Controlla</th></tr></thead><tbody><tr><td><code>clearance-permissions-write</code></td><td>Creazione / modifica / eliminazione dei permessi</td></tr><tr><td><code>clearance-roles-write</code></td><td>Creazione / modifica / eliminazione dei ruoli e delle loro assegnazioni di permessi</td></tr><tr><td><code>clearance-guards-write</code></td><td>Creazione / modifica / eliminazione delle guardie</td></tr><tr><td><code>clearance-settings-write</code></td><td>Modifica dei metadati di visualizzazione, icone, colori, ruolo predefinito</td></tr><tr><td><code>clearance-users-write</code></td><td>Assegnazione / rimozione dei ruoli per uno specifico utente</td></tr></tbody></table><p>Se una riga di permesso granulare non esiste affatto nel database (uno stato legacy pre-seeding), il controllo ricade su <code>clearance-access</code> per retrocompatibilità.</p><h2 id="namespace-clearance-protetto" tabindex="-1">Namespace <code>clearance-*</code> protetto <a class="header-anchor" href="#namespace-clearance-protetto" aria-label="Permalink to &quot;Namespace `clearance-*` protetto&quot;">​</a></h2><p>I permessi <code>clearance-*</code> sono riservati: non possono essere creati, rinominati, eliminati, o aggiunti a nessun ruolo se non attraverso il flusso di installazione proprio del pacchetto - nemmeno da un attore <code>super_admin</code> per il ruolo <code>super_admin</code> stesso.</p><h2 id="limiti-ceiling-contro-l-escalation-di-privilegi" tabindex="-1">Limiti ceiling contro l&#39;escalation di privilegi <a class="header-anchor" href="#limiti-ceiling-contro-l-escalation-di-privilegi" aria-label="Permalink to &quot;Limiti ceiling contro l&#39;escalation di privilegi&quot;">​</a></h2><p>Ogni percorso di concessione dei permessi in Clearance applica un ceiling in modo che un attore non possa mai concedere più di quanto detiene lui stesso:</p><ul><li><strong>Ruoli ceiling</strong> - un ruolo può dichiarare un ruolo genitore il cui set di permessi funge da limite superiore. Ogni permesso in eccesso viene rimosso silenziosamente su <code>RoleService::setParent()</code> e su ogni successiva sincronizzazione dei permessi - nessuna eccezione, nessun messaggio di errore. La rimozione di permessi da un genitore propaga automaticamente la rimozione a ogni figlio. Vedi <a href="/clearance-docs/it/features/roles.html">Roles</a> per l&#39;API completa del ceiling.</li><li><strong>Modulo Utenti</strong> - sia l&#39;assegnazione dei ruoli che le concessioni dirette/override dei permessi applicano un ceiling di privilegio: un attore non <code>super_admin</code> non può modificare le proprie assegnazioni, e non può concedere un permesso che non detiene lui stesso. Vedi <a href="/clearance-docs/it/features/users.html">Users</a>.</li></ul><h2 id="override-di-contesto-con-priorita-al-diniego" tabindex="-1">Override di contesto con priorità al diniego <a class="header-anchor" href="#override-di-contesto-con-priorita-al-diniego" aria-label="Permalink to &quot;Override di contesto con priorità al diniego&quot;">​</a></h2><p>Gli override dei permessi per contesto (<code>clr_ctx_overrides</code>) supportano <code>forced_on</code> e <code>forced_off</code>. <code>forced_off</code> vince sempre - anche su un permesso concesso da un ruolo globale. Non esiste un percorso che permetta a una concessione di priorità inferiore di prevalere su un diniego esplicito. Vedi <a href="/clearance-docs/it/guide/architecture.html#how-resolution-works">Architecture</a> per l&#39;ordine completo di risoluzione.</p><h2 id="sanitizzazione-svg" tabindex="-1">Sanitizzazione SVG <a class="header-anchor" href="#sanitizzazione-svg" aria-label="Permalink to &quot;Sanitizzazione SVG&quot;">​</a></h2><p>Ogni icona fornita dall&#39;utente (metadati di visualizzazione di ruolo/guardia nel pannello Impostazioni) passa attraverso un sanitizzatore allow-list sia prima della memorizzazione sia in fase di rendering - non un controllo una tantum al momento del caricamento.</p><h2 id="direttive-blade-sicure-per-gli-ospiti" tabindex="-1">Direttive Blade sicure per gli ospiti <a class="header-anchor" href="#direttive-blade-sicure-per-gli-ospiti" aria-label="Permalink to &quot;Direttive Blade sicure per gli ospiti&quot;">​</a></h2><p><code>@canin</code> e <code>@hasrolein</code> si risolvono entrambe in modo sicuro (restituiscono <code>false</code>) per un utente ospite - non lanciano mai un&#39;eccezione.</p><h2 id="il-bypass-del-gate-e-a-livello-di-applicazione" tabindex="-1">Il bypass del Gate è a livello di applicazione <a class="header-anchor" href="#il-bypass-del-gate-e-a-livello-di-applicazione" aria-label="Permalink to &quot;Il bypass del Gate è a livello di applicazione&quot;">​</a></h2><p><code>super_admin_gate_bypass</code> (predefinito <code>false</code>) controlla se il ruolo <code>super_admin</code> bypassa tutti i controlli Laravel <code>can()</code> / <code>Gate::allows()</code> / delle policy a livello globale, tramite un hook <code>Gate::before()</code>.</p><div class="warning custom-block"><p class="custom-block-title">Non limitato al pannello</p><p>Quando abilitato, qualsiasi utente che detiene <code>super_admin</code> salta tutti i controlli Gate a livello di applicazione - non solo i controlli del pannello Clearance. Abilitalo deliberatamente. Vedi <a href="/clearance-docs/it/features/super-admin.html">Super Admin</a> per il rilevamento degli alias e il comportamento di promozione.</p></div><h2 id="accortezze-rilevanti-per-la-sicurezza" tabindex="-1">Accortezze rilevanti per la sicurezza <a class="header-anchor" href="#accortezze-rilevanti-per-la-sicurezza" aria-label="Permalink to &quot;Accortezze rilevanti per la sicurezza&quot;">​</a></h2><ul><li><code>forced_off</code> vince sempre - anche su un permesso concesso da un ruolo globale.</li><li>Un ruolo ceiling rimuove soltanto, non concede mai - impostare un genitore permissivo non concede di per sé alcun permesso al figlio.</li><li>Il bypass del Gate (<code>super_admin_gate_bypass</code>) è a livello di applicazione, non limitato al pannello.</li><li>Il modulo Utenti è disattivato per impostazione predefinita (<code>modules.users = false</code>).</li><li>I permessi <code>clearance-*</code> sono intoccabili al di fuori del flusso di installazione, anche per <code>super_admin</code>.</li><li>Esegui sempre <code>clearance:install</code>, non una semplice <code>migrate</code>, su un&#39;installazione nuova - vedi <a href="/clearance-docs/it/guide/installation.html">Installation</a>.</li></ul><h2 id="correlati" tabindex="-1">Correlati <a class="header-anchor" href="#correlati" aria-label="Permalink to &quot;Correlati&quot;">​</a></h2><ul><li><a href="/clearance-docs/it/guide/architecture.html">Architecture</a> - precedenza di risoluzione e schema <code>clr_*</code></li><li><a href="/clearance-docs/it/features/roles.html">Roles</a> - i ruoli ceiling in dettaglio</li><li><a href="/clearance-docs/it/features/users.html">Users</a> - il ceiling di privilegio sull&#39;assegnazione di ruoli/permessi</li><li><a href="/clearance-docs/it/features/super-admin.html">Super Admin</a> - bypass del Gate e rilevamento degli alias</li></ul>',24)])])}const g=i(t,[["render",l]]);export{m as __pageData,g as default};
