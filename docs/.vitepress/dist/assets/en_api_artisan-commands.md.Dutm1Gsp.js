import{_ as a,o as s,c as i,a2 as t}from"./chunks/framework.BKG_UbFK.js";const k=JSON.parse('{"title":"Artisan Commands","description":"","frontmatter":{},"headers":[],"relativePath":"en/api/artisan-commands.md","filePath":"en/api/artisan-commands.md"}'),n={name:"en/api/artisan-commands.md"};function l(r,e,o,d,c,h){return s(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="artisan-commands" tabindex="-1">Artisan Commands <a class="header-anchor" href="#artisan-commands" aria-label="Permalink to &quot;Artisan Commands&quot;">​</a></h1><p>Clearance ships two Artisan commands: <code>clearance:install</code> for first-time setup, and <code>clearance:backfill</code> for seeding defaults on existing installs. See the <a href="/clearance-docs/en/guide/installation.html">Installation guide</a> for the end-to-end setup walkthrough.</p><h2 id="clearance-install" tabindex="-1"><code>clearance:install</code> <a class="header-anchor" href="#clearance-install" aria-label="Permalink to &quot;\`clearance:install\`&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clearance:install</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clearance:install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --user=1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clearance:install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --role=editor</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clearance:install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --super-admin-role=owner</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clearance:install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --force</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Flag</th><th>Description</th></tr></thead><tbody><tr><td><code>--user=ID</code></td><td>Assign the <code>super_admin</code> role to a user after install.</td></tr><tr><td><code>--role=NAME</code></td><td>Assign <code>clearance-access</code> to a role (created if absent).</td></tr><tr><td><code>--super-admin-role=NAME</code></td><td>Promote an existing role to <code>super_admin</code> (skips the interactive prompt).</td></tr><tr><td><code>--force</code></td><td>Re-run even if already installed.</td></tr></tbody></table><h3 id="what-it-does-in-order" tabindex="-1">What it does, in order <a class="header-anchor" href="#what-it-does-in-order" aria-label="Permalink to &quot;What it does, in order&quot;">​</a></h3><ol><li>Publishes <code>config/clearance.php</code>.</li><li>Publishes Clearance&#39;s migrations.</li><li>Detects Spatie Permission tables — if the <code>roles</code> table is absent, publishes and runs Spatie&#39;s migrations automatically first.</li><li>Runs all pending migrations.</li><li>Creates <code>clearance-access</code> and all 5 fine-grained capability permissions: <code>clearance-permissions-write</code>, <code>clearance-roles-write</code>, <code>clearance-guards-write</code>, <code>clearance-settings-write</code>, <code>clearance-users-write</code>.</li><li>Creates the <code>super_admin</code> role with all <code>clearance-*</code> permissions assigned. This step is additive — it never removes existing permissions on re-run.</li><li>Writes <code>storage/.clearance-installed</code> as an idempotency marker.</li></ol><h3 id="alias-detection" tabindex="-1">Alias detection <a class="header-anchor" href="#alias-detection" aria-label="Permalink to &quot;Alias detection&quot;">​</a></h3><p>If the database already contains a role named <code>super_admin</code>, <code>superadmin</code>, <code>super-admin</code>, <code>root</code>, or <code>owner</code>, the installer detects it and prompts interactively to either promote it or create a separate <code>super_admin</code> role. Use <code>--super-admin-role=NAME</code> to skip the prompt non-interactively.</p><p>Promotion renames the role in place — all <code>model_has_roles</code> assignments are preserved via the foreign key on <code>role_id</code>.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p><code>clearance-*</code> permissions are protected outside the install flow — see <a href="/clearance-docs/en/api/services.html#permissionservice"><code>PermissionService</code></a> and the <code>ClearanceProtectedResourceException</code> in <a href="/clearance-docs/en/api/services.html#exceptions">Services</a>.</p></div><h2 id="clearance-backfill" tabindex="-1"><code>clearance:backfill</code> <a class="header-anchor" href="#clearance-backfill" aria-label="Permalink to &quot;\`clearance:backfill\`&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clearance:backfill</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">              # meta + role scope + guards</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clearance:backfill</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --only=meta</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # just one section</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clearance:backfill</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dry-run</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # preview, no writes</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Flag</th><th>Description</th></tr></thead><tbody><tr><td><code>--only=meta|roles|guards</code></td><td>Run only one backfill section.</td></tr><tr><td><code>--dry-run</code></td><td>Preview changes without writing.</td></tr></tbody></table><h3 id="sections" tabindex="-1">Sections <a class="header-anchor" href="#sections" aria-label="Permalink to &quot;Sections&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Section</th><th>Description</th></tr></thead><tbody><tr><td><code>meta</code></td><td>Seeds a <code>clr_meta</code> display name (title-cased role name) for every role without one.</td></tr><tr><td><code>roles</code></td><td>Seeds <code>clr_role_meta</code> defaults (<code>scope=global</code>, <code>is_locked=false</code>) for every role without one.</td></tr><tr><td><code>guards</code></td><td>Imports guards from <code>config(&#39;auth.guards&#39;)</code> into <code>clr_guards</code>, filtered by <code>allowed_guard_drivers</code>.</td></tr></tbody></table><p>All sections are idempotent — running <code>clearance:backfill</code> repeatedly is safe and only fills in what&#39;s missing.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>For pre-existing Spatie installs: <code>clearance:install</code> never runs Spatie&#39;s migrations if the <code>roles</code> table already exists, so existing roles, permissions, and assignments are preserved automatically.</p></div><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="/clearance-docs/en/api/services.html">Services</a> — <code>GuardService</code> and <code>MetaService</code>, which back the <code>guards</code> and <code>meta</code> backfill sections.</li><li><a href="/clearance-docs/en/guide/migration.html">Migration guide</a> — migrating an existing Spatie Permission installation to Clearance.</li></ul>`,20)])])}const m=a(n,[["render",l]]);export{k as __pageData,m as default};
